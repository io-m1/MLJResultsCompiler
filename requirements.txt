openpyxl==3.1.5
# pandas 2.2.x has prebuilt wheels for Python 3.13 (avoids source build failures)
pandas==2.2.3
python-docx==0.8.11
reportlab==4.0.9
python-telegram-bot==20.3
python-dotenv==1.0.0
fastapi==0.110.0
uvicorn[standard]==0.23.2
Pillow==11.0.0
httpx==0.27.0

# Optional dependencies for conversational document processing
# Uncomment the following to enable advanced features:
# pytesseract>=0.3.10  # For OCR from images
# pdf2image>=1.16.3    # For PDF to image conversion
# pdfplumber>=0.10.3   # For PDF text/table extraction
# opencv-python>=4.8.1 # For advanced image processing
# tabula-py>=2.8.2     # For table extraction from PDFs
